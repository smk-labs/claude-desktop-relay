{
  "name": "claude-desktop-relay",
  "version": "0.9.0-beta.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "node --test --test-timeout=20000 \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "relay": "node scripts/relay.ts",
    "package": "node scripts/package.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  },
  "description": "One Claude Desktop window, several of your own Claude subscriptions behind it.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/smk-labs/claude-desktop-relay.git"
  }
}
